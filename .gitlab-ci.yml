stages:
  - test
  - build

test-golint:
  stage: test
  image: golangci/golangci-lint:v1.27.0
  script:
    - golangci-lint run -v

build-develop-api:
  stage: build
  image: docker:latest
  only:
    refs:
      - master
    changes:
      - Dockerfile-api
      - cmd/api/**
  services:
    - docker:dind
  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
  script:
    - docker build --pull -t "$CI_REGISTRY/kube-autodiscover-api:latest" -f Dockerfile-api .
    - docker tag "$CI_REGISTRY/kube-autodiscover-api:latest" "$CI_REGISTRY/kube-autodiscover-api:develop-$CI_COMMIT_SHORT_SHA"
    - docker push "$CI_REGISTRY/kube-autodiscover-api:latest"
    - docker push "$CI_REGISTRY/kube-autodiscover-api:develop-$CI_COMMIT_SHORT_SHA"

build-develop-poster:
  stage: build
  image: docker:latest
  only:
    refs:
      - master
    changes:
      - Dockerfile-poster
      - cmd/poster/**
  services:
    - docker:dind
  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
  script:
    - docker build --pull -t "$CI_REGISTRY/kube-autodiscover-poster:latest" -f Dockerfile-poster .
    - docker tag "$CI_REGISTRY/kube-autodiscover-poster:latest" "$CI_REGISTRY/kube-autodiscover-poster:develop-$CI_COMMIT_SHORT_SHA"
    - docker push "$CI_REGISTRY/kube-autodiscover-poster:latest"
    - docker push "$CI_REGISTRY/kube-autodiscover-poster:develop-$CI_COMMIT_SHORT_SHA"

build-develop-cli:
  stage: build
  image: golang:1.16.0-alpine3.13
  only:
    refs:
      - master
      - tags
    changes:
      - cmd/b2k/**
  script:
    - go mod download
    - go mod verify
    - CGO_ENABLED=0 GOOS=darwin GOARCH=amd64 go build -ldflags="-w -s -X main.sha1=$CI_COMMIT_SHA -X main.buildTime=$(date +'%Y-%m-%dT%T') -X main.version=$CI_COMMIT_TAG" -o b2k cmd/b2k/main.go
    - CGO_ENABLED=0 GOOS=linux GOARCH=386 go build -ldflags="-w -s -X main.sha1=$CI_COMMIT_SHA -X main.buildTime=$(date +'%Y-%m-%dT%T') -X main.version=$CI_COMMIT_TAG" -o b2k_linux_386 cmd/b2k/main.go
    - CGO_ENABLED=0 GOOS=android GOARCH=arm64 go build -ldflags="-w -s -X main.sha1=$CI_COMMIT_SHA -X main.buildTime=$(date +'%Y-%m-%dT%T') -X main.version=$CI_COMMIT_TAG" -o b2k_android_arm64 cmd/b2k/main.go
  artifacts:
    paths:
      - b2k
      - b2k_linux_386
      - b2k_android_arm64
